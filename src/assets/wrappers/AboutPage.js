import styled from 'styled-components'

const Wrapper = styled.section`
  h3 {
    font-weight: 600;
  }
  p {
    line-height: 2;
    color: var(--grey-500);
    margin-top: 2rem;
  }
`

export default Wrapper
